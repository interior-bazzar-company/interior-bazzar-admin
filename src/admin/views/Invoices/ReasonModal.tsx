/* CONFIRM, AND SAY WHY (cancel an issued invoice, remove a payment proof).

   This module and its twin each carried a byte-identical copy of this dialog.
   There is one drawing now, in `ui/overlays`; this file stays only so the
   module's own import path keeps working. */
export { ReasonModal as default } from "../../ui";
